<% 
let count = 0;
bots.forEach((bot) => {
    if (count % 3 === 0) { 
%>
<div class="row">
<%
}
%>
  <div class="col s12 m6 l4 xl3">
    <div class="card">
      <div class="card-image">
        <img class="responsive-img" src="<%= bot.img %>">
        <span class="card-title"><%= bot.name %></span>
        <a class="btn-floating halfway-fab waves-effect waves-light purple"><i class="material-icons">chat_bubble</i></a>
      </div>
      <div class="card-content">
        <p><%= bot.advice %></p>
      </div>
    </div>
  </div>
<%
if (count % 3 === 2) { 
%>
</div>
<%
count++;
}
}); 
%>  